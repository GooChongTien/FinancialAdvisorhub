version: 1
defaults:
  continue_on_error: false
  max_retries: 1
pipelines:
  input:    [pii_detect, moderation, jailbreak]
  toolcall: [permission_gate, change_risk, confirm_if_high]
  output:   [hallucination_check, pii_leak, moderation]
